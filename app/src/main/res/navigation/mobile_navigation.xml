<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_gate">

    <fragment
        android:id="@+id/navigation_gate"
        android:name="com.duimane.gatecontrol.fragment.GateFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_gate" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.duimane.gatecontrol.fragment.SettingsFragment"
        android:label="@string/title_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>